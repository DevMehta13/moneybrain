# Android / Gradle (populated when the project skeleton lands in phase 0)
.gradle/
.kotlin/
build/
local.properties
.idea/
*.keystore
*.jks
captures/

# OS noise
.DS_Store

# Personal Claude Code settings (never commit — repo is public)
.claude/

# Never commit anything personal: masked-SMS staging, tokens, exports
private/
*.secret
